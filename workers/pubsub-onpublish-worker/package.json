{
  "name": "pubsub-onpublish-worker",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "format": "prettier . --write",
    "dev": "wrangler dev src/index.ts",
    "deploy": "npm test && wrangler publish",
    "test": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/pubsub": "^0.0.1",
    "@cloudflare/workers-types": "^4.20230228.0",
    "@types/node": "^18.14.5",
    "vitest": "^0.29.2",
    "wrangler": "^2.12.0"
  },
  "prettier": "prettier-config-standard"
}
